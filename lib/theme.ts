import { createTheme } from "@mui/material/styles"

const theme = createTheme({
  palette: {
    primary: {
      main: "#2196f3", // A shade of blue for primary color
    },
    secondary: {
      main: "#FF9D16", // A different shade of blue for secondary color
    },
    background: {
      default: "#e3f2fd", // A light blue background color
    },
  },
})
export default theme
