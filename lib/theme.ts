import { createTheme } from "@mui/material/styles"

const theme = createTheme({
  palette: {
    primary: {
      main: "#37B8E2", // A shade of blue for primary color
    },
    secondary: {
      main: "#00329C", // A different shade of blue for secondary color
    },
    background: {
      default: "#e3f2fd", // A light blue background color
    },
  },
})
export default theme
